#include<stdio.h>
#include<cs50.h>

int main (void)
{

  int array[100], swap, n, c, pst, x, y;

  n= get_int("Enter the number of element: ");

  printf("Enter those elements one by one.");

  for(c=0; c<n; c++)
  {
  int x=get_int("Enter a number: ");
  printf("%d\n", array[c]);
  }

  for(c=0; c < n - 1; c++)
  {
      pst=c;

      for(y=0; y<n; y++)
      {
          pst=y;
          if(c<=(c+=))
          {}
      }
  }


  }