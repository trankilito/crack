#include<stdio.h>
#include<cs50.h>

void swap (int *a, int *b)
{
    int tmp = *a;
    *a = *b;
    *b = tmp;
}

int	main(void)
{
    a = 2;m  c2;
    b = 6;
    int	tmp	=	*a;
    *a	= *b;
    *b	= tmp;

    printf("%i",tmp);
}

