#include<stdio.h>
#include<cs50.h>

int ops (int a, int b, char signe[i]);

int main (void)
{   char i;
    int result= ops(a,b);
    char signe[4]={+,-,*,/};


}

int ops (int a, int b)
{
    return a signe[i] b;
}