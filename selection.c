#include<stdio.h>
#include<cs50.h>

int main (void)
{

  int tab[100], n,index,index1,index2, c;
  int x=0;
  n= get_int("Enter the number of element: ");

for(int e=0; e<n; e++)
  {
   tab[x]=get_int("Enter a number: ");
  printf("%d\n", array[x]);
  }

for(index=0; index < n; index++)
{
  n= tab[index];
  index1= index;
  for(index2=1; index2 < n; index2++)
  {
    if (tab[index2]< n)
    {
      index select= index2;
      n= tab[index2];

    }
    int result= tab[index]
  printf("%i\n", result);
  }


}